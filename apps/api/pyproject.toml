[project]
name = "stagebrain-api"
version = "0.1.0"
description = "StageBrain — real-time decision support for live shows"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.34",
]
