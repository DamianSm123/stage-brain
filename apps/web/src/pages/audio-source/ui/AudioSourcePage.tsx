import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/shared/ui/card";

export function AudioSourcePage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Audio Source</CardTitle>
        <CardDescription>Manage audio input sources and configuration.</CardDescription>
      </CardHeader>
      <CardContent>
        <p className="text-sm text-muted-foreground">Audio Source module — coming soon.</p>
      </CardContent>
    </Card>
  );
}
